// src/components/ProtectedRoute.jsx
import React from "react";

export default function ProtectedRoute({ children }) {
  // Por ahora, permitir acceso directo sin autenticación
  return children;
}
